{% if site.url %}
  {% assign base_path = site.url | append: site.baseurl %}
{% else %}
  {% assign base_path = site.github.url %}
{% endif %}
{% assign current_locale = page.locale | default: page.lang | default: site.locale %}
